# Python bytecode caches
__pycache__/
*.pyc
*.pyo

# Build artifacts
build/
dist/
*.spec.bak

# Local user data — created by DCSAutoMate at runtime
DCSAutoMateConfig.json
DCSAutoMateSettings.json
DCSAutoMateLog.txt

# Local release artifacts (uploaded to GitHub releases, not tracked in repo)
release-v*/
